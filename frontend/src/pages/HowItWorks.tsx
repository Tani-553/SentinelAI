import { PageTitle } from "./RiskAnalysis";
const steps = [["01", "Transaction Data", "An anonymized transaction sample is selected for evaluation."], ["02", "Feature Processing", "The transaction data is prepared using the trained preprocessing pipeline."], ["03", "Machine Learning Assessment", "The trained model calculates the probability of fraudulent activity."], ["04", "Risk Strategy", "The selected business risk mode determines the decision threshold."], ["05", "Clear Decision", "SentinelAI provides an APPROVE or BLOCK recommendation."]];
export function HowItWorks() { return <section className="page-section"><PageTitle kicker="PROCESS OVERVIEW" title="How SentinelAI Works" description="From transaction data to an actionable fraud decision." /><div className="process-flow">{steps.map(([number, title, description], index) => <div className="flow-unit" key={number}><article className="flow-card"><span>{number}</span><div><h3>{title}</h3><p>{description}</p></div></article>{index < steps.length - 1 && <i className="flow-arrow">↓</i>}</div>)}</div></section>; }
