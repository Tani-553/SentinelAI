import { riskModes } from "../siteData";
import { PageTitle } from "./RiskAnalysis";
export function RiskStrategies() { return <section className="page-section"><PageTitle kicker="CONFIGURABLE RISK POSTURE" title="Adaptive Risk Strategies" description="Different businesses have different priorities. SentinelAI allows fraud decisions to adapt to the selected risk strategy." /><div className="strategy-grid">{riskModes.map((mode) => <article className="strategy-card" key={mode.value}><span className="threshold">Threshold: {mode.threshold}</span><h2>{mode.label}</h2><p>{mode.description}</p><small>Real threshold used by the application</small></article>)}</div><div className="comparison"><div><p className="kicker">AT A GLANCE</p><h2>Threshold comparison</h2><p>Lower thresholds are more sensitive to potential fraud. Higher thresholds reduce unnecessary blocks.</p></div><div className="comparison-table"><div className="table-row table-head"><span>Strategy</span><span>Threshold</span><span>Priority</span></div>{riskModes.map((mode) => <div className="table-row" key={mode.value}><strong>{mode.label}</strong><span>{mode.threshold}</span><span>{mode.value === "fraud_protection" ? "Detection sensitivity" : mode.value === "balanced" ? "Balanced outcomes" : "Customer continuity"}</span></div>)}</div></div></section>; }
